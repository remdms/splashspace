<nav class="navbar sticky-top">
  <div class="navbar__left">
    <%= link_to root_path, class: "icon-link" do %>
      <i class="fas fa-sliders-h"></i>
    <% end %>
  </div>

  <div class="navbar__center">
    <%= link_to "SPLASHSPACE", root_path, class: "nav-title" %>
  </div>

  <div class="navbar__right">
    <% if user_signed_in? && current_user.photo.attached? %>
      <%= link_to user_path(current_user), class: "profile-link" do %>
        <%= cl_image_tag current_user.photo.key, class: "avatar-rounded" %>
      <% end %>
    <% else %>
      <%= image_tag("default-profile-picture.png", class: "avatar-rounded") %>
    <% end %>
  </div>
</nav>
